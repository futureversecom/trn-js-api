{
  "name": "@therootnetwork/evm",
  "version": "1.0.5",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "prettier --config ../../.prettierrc.js --ignore-path ../../.prettierignore --check . && eslint . --ext .ts  --ignore-path ../../.eslintignore",
    "test": "jest -c ../../jest.config.js evm/tests",
    "test:watch": "jest -c ../../jest.config.js evm/tests --watch --coverage"
  },
  "devDependencies": {
    "ethers": "^6.6.5"
  }
}
