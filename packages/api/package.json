{
  "name": "@therootnetwork/api",
  "version": "1.0.6",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "Apache-2.0",
  "peerDependencies": {
    "@polkadot/api": "^10.8.1"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "prettier --config ../../.prettierrc.js --ignore-path ../../.prettierignore --check . && eslint . --ext .ts  --ignore-path ../../.eslintignore",
    "test": "jest -c ../../jest.config.js .*.test.ts",
    "jest": "jest -c ../../jest.config.js",
    "test:watch": "jest -c ../../jest.config.js --watch --coverage"
  }
}
