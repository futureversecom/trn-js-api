{
  "name": "@therootnetwork/api-types",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "peerDependencies": {
    "@polkadot/types": "^10.9.1"
  },
  "scripts": {
    "_clean": "rm -rdf dist",
    "_build": "tsc --build tsconfig.json",
    "build": "run-s _clean _build",
    "lint": "dprint check && eslint . --ext .ts",
    "format": "dprint fmt --incremental",
    "upd:meta": "ts-node scripts/updateMeta.ts",
    "_gen": "ts-node scripts/generateTypes.ts",
    "gen": "run-s _gen format"
  },
  "devDependencies": {
    "@polkadot/api-base": "^10.9.1",
    "@polkadot/rpc-core": "^10.9.1",
    "@polkadot/typegen": "^10.9.1",
    "@polkadot/types-codec": "^10.9.1",
    "@polkadot/types-create": "^10.9.1",
    "@types/websocket": "^1.0.5",
    "handlebars": "^4.7.8",
    "websocket": "^1.0.34"
  }
}
